<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- MainActivity（桌面启动）暗色变体：黑底 splash，避免深色模式下冷启动白闪。 -->
    <style name="MainTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- ExternalDownloadActivity（外部下载唤起）暗色变体：保持透明，弹窗透出来源应用。 -->
    <style name="ExternalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
    </style>
</resources>